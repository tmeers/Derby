@using Derby.Infrastructure
@using Derby.Models
@model Derby.ViewModels.PackViewModel

@{
    ViewBag.Title = "Details";
}

<h3>Pack: @Html.DisplayFor(model => model.Name) - <small>@Html.DisplayFor(model => model.Region)</small></h3>
<p>
    @if (Model.Membership.AccessLevel == OwnershipType.Owner || Model.Membership.AccessLevel == OwnershipType.Contributor)
    {
        <a href="@Url.Action("Index", "Scout", new { packId = Model.Id })" class="btn btn-xs btn-primary" title="Delete"><i class="icon-group"></i>&nbsp; Scouts</a>
        <a href="@Url.Action("create", "Competition", new { packId = Model.Id })" class="btn btn-xs btn-info" title="Delete"><i class="icon-trophy"></i>&nbsp; New Competition</a>
    }
</p>
<div>
    <div class="row">
        <div class="col-md-5">

            <table class="table">
                <tr>
                    <th colspan="4">
                        @Html.DisplayNameFor(model => model.Competitions)
                    </th>
                </tr>
                <tr>
                    <th>
                        Event Date
                    </th>
                    <th>
                        Title
                    </th>
                    <th>
                        Racers
                    </th>
                    <th>&nbsp;</th>
                </tr>
                @foreach (var comp in Model.Competitions)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => comp.EventDate)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => comp.Title)
                        </td>
                        <td>
                            @* // TODO Add Racers count *@
                        </td>
                        <td>
                            @Html.ActionLink("Details", "Details", "Competition", new { id = comp.Id }, new { @class = "btn btn-xs btn-default" })

                            @*@if (Model.Membership.AccessLevel == OwnershipType.Owner)
                            {
                                @:&nbsp; | &nbsp;
                                @Html.ActionLink("Delete", "Delete", "Competition", new { id = comp.Id }, new { @class = "btn btn-xs btn-danger" })
                            }*@
                        </td>
                    </tr>
                }

            </table>
        </div>
        <div class="col-sm-1">
            <table class="table">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Dens)
                    </th>
                    <th>
                        @Html.ActionLink("Add Den", "Create", "Den", new { packId = Model.Id }, new { @class = "btn btn-xs btn-success" })
                    </th>
                    <th></th>
                </tr>

                @foreach (var den in Model.Dens)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => den.Name)
                        </td>
                        <td>
                            <a href="@Url.Action("Edit", "Den", new { id = den.Id })" class="btn btn-xs btn-primary" title="Edit"><i class="icon-pencil"></i></a>

                            @if (Model.Membership.AccessLevel == OwnershipType.Owner)
                            {
                                <a href="@Url.Action("Delete", "Den", new { id = den.Id })" class="btn btn-xs btn-danger" title="Delete"><i class="icon-trash"></i></a>
                            }
                        </td>
                    </tr>
                }

            </table>
        </div>
    </div>
</div>
<hr/>
<div>
    @Html.ActionLink("Edit", "edit", new { id = Model.Id }, new { @class = "btn btn-xs btn-primary" }) |
    @Html.ActionLink("Back to List", "index", new { }, new { @class = "btn btn-xs btn-default" })
</div>